apiVersion: v1
kind: Service
metadata:
  name: gpu-inference-api
  namespace: gpu-inference
  labels:
    app: gpu-inference-api
spec:
  selector:
    app: gpu-inference-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
